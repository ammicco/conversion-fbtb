#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "manipulate_string.h"

int decimal_to_base(int, int);

int base_to_decimal(int, int);

int base_to_base(int, int, int);

char *decimal_to_base_upper_10(int, int);

int base_upper_10_to_decimal(char *, int);

int base_lenght(int, int);

int cifre(int);

int is_actual_this_base(int, int);

int is_actual_this_base_upper_10(char *, int);